{
  "name": "black-jack-backend",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "start": "dotnet run --launch-profile http",
    "start:https": "dotnet run --launch-profile https",
    "watch": "dotnet watch run --launch-profile http",
    "build": "dotnet build",
    "publish": "dotnet publish -c Release -o ./publish",
    "test": "dotnet test"
  }
}
